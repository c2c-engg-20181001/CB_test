var config = require('../../config');
var compare = require('./Compare-Controller');

function azureFunction(cloud_name) {
    var cloud = 'testData';
    return cloud;
}
exports.azureFunction = azureFunction;
